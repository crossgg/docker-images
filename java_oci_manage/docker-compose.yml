version: "3"
services:
  oci:
    image: crosscc/java_oci_manage
    container_name: Rbot
    tty: true
    ports:
      - 9527:9527
    volumes:
      #  - ./config/client_config:/app/client_config
      #  - ./config/key.pem:/app/key.pem
      - ./app:/app/
    restart: unless-stopped
